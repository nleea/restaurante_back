"""Staff module: employees, planned shifts, attendances and commissions.

Maps the workforce side of a branch: who works there (`employees`, linking a
`person` and a `user` to a `role`), their scheduling (`planned_shifts`), the
actual clock-in/out records (`attendances`) and the money earned on top of base
pay (`commissions`).
"""
