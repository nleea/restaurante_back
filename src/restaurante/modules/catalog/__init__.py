"""Catalog module: global reference data (countries, cities, units of measure).

These are system-wide catalogs shared by every tenant, so they are NOT
tenant-scoped (plain `Base`, no `tenant_id`).
"""
