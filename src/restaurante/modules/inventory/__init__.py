"""Inventory module: branch-scoped stock levels and stock movements.

Tracks the on-hand quantity of each ingredient per branch (`inventory_stocks`)
and the audit log of every change to that quantity (`inventory_movements`).
"""
